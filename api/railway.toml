# railway.toml — Railway deployment config for the NestJS API
[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node dist/main"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
